Special playlists (All, untagged)
async operations in getresult

installer does not provide access to the folders
maybe replace listboxes with buttons with itemscontrol to use buttons and that can be disabled
check if the Resource blocks at the start of views are needed
graph editor: nodes attach to mouse if mouse is released over e.g. a textbox while dragging a node